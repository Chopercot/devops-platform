apiVersion: v1
kind: Service
metadata:
  name: devops-service
  namespace: devops-platform

spec:
  selector:
    app: devops-app
    version: green   # 👈 ВАЖНО
  ports:
    - port: 80
      targetPort: 8080
